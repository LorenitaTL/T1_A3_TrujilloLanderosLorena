import scala.io.StdIn._

object Vocales {
  val vocales = "AEIOU"
  def vocales(pal: String):Int={
    pal.toUpperCase()
    if(pal==null){
      0
    }else{
      
    }
    5
  }
  
  def main(args: Array[String]): Unit ={
  println("Ingresa la palabra que deseas analizar")
  val palabra = readLine()
  }
}